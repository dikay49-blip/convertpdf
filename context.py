"""
Context processor — inject `now` into all templates.
Add this at the bottom of app.py (already included via import below).
"""
# This file is a reminder: in app.py, the context processor is already inline.
# See the @app.context_processor decorator in app.py.
